-Check What Is Using Port 8080
sudo lsof -i :8080

-Kill That Process
kill -9 12345

------

git config --global user.name "William-ticket-1"
git config --global user.email "thinhproee@gmai.com"
------

Go installs binaries to:
code ~/.bashrc

---
